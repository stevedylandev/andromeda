services:
  feeds:
    build:
      context: ../..
      dockerfile: apps/feeds/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - feeds_data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  feeds_data:
