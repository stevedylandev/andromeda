services:
  parcels:
    build:
      context: ../..
      dockerfile: apps/parcels/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - parcels_data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  parcels_data:
