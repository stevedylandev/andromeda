services:
  app:
    build:
      context: ../..
      dockerfile: apps/shrink/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
