services:
  app:
    build:
      context: ../..
      dockerfile: apps/shrink-go/Dockerfile
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - HOST=0.0.0.0
      - PORT=${PORT:-3000}
    restart: unless-stopped
